#include "main.h"
/**
 * _abs - Write a function that computes the absolute value of an integer.
 * @ab: integer input
 * Author Tway
 * Return: value of ab (success)
 */
int _abs(int ab)
{
return (ab * ((ab > 0) - (ab < 0)));
}
