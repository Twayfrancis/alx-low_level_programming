#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 * @b: first input
 * @c: second input
 * Return: the sum of b and c
 */
int add(int b, int c)
{
return (b + c);
}
