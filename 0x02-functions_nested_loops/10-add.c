#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 * @one: first input
 * @two: second input
 * Return; Always 0
 */
int add(int one, int two)
{
return (one + two);
}
